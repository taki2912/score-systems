# 计分系统

基于 Flask 的俱乐部计分管理系统

## 部署到 Render

本项目已配置好 Render 部署文件，可直接部署到 Render.com 获得固定域名。

## 本地运行

```bash
pip install -r requirements.txt
python app.py
```

访问 http://localhost:5000

默认管理员账号：admin / admin123
